# OPAL Update 131.7 R124
## السلطة الوحيدة النهائية لملف CSS

### نتيجة الفحص العميق

تم العثور على مصادر بصرية متعددة كانت تفسر بقاء بعض الألوان أو الأنماط رغم تغيير الثيم، وأهمها:

1. كتل `<style>` داخل قوالب النظام.
2. نقطة `{% block extra_css %}` في القالب الأساسي، والتي كانت تسمح للقوالب الفرعية بإضافة CSS بعد/حول سلطة الثيم.
3. أنماط `style=""` ثابتة داخل القوالب.
4. قواعد قديمة داخل ملف CSS نفسه، بما فيها ألوان داكنة صريحة.
5. مؤشرات الرضا كانت تستخدم قرصًا داخليًا داكنًا ثابتًا، ولذلك كان الرقم داخل الحلقة يتأثر بتباين الثيم الأخضر.
6. ألوان مخطط الإيرادات كانت مكتوبة داخل JavaScript؛ لذلك بقيت هذه المنطقة مصدر لون مستقل عن CSS.

### ما تم في R124

- أصبح `opal_theme_system.css` ملف CSS الوحيد المملوك لـOPAL.
- أزيلت نقطة `extra_css` من `base.html`.
- نُقلت كتل `<style>` إلى ملف السلطة.
- حُولت الأنماط الثابتة المضمنة إلى classes داخل ملف السلطة.
- الصفحات المستقلة وصفحات الطباعة أصبحت مرتبطة بملف CSS نفسه.
- أضيفت حماية اختبارية تمنع عودة `<style>` أو ألوان ثابتة داخل `style=""`.
- تم إصلاح مؤشرات الرضا بحيث تستخدم ألوان الثيم الحالية للقرص الداخلي والنص.
- تم إبقاء القيم الديناميكية التي تأتي من البيانات، مثل عرض شريط التقدم أو لون المادة/المقرر، كبيانات تشغيلية؛ أما طريقة عرضها فهي في CSS.
- تم الحفاظ على Bootstrap كاعتماد إطار عمل فقط، وليس كمصدر هوية OPAL.

### ملاحظة مهمة

لم يتم حذف CSS التاريخي الموجود داخل ملف السلطة نفسه بصورة عشوائية؛ لأن ذلك قد يكسر وظائف أو صفحات قديمة. بدلاً من ذلك أصبحت هناك طبقة نهائية واحدة تحسم القيم المرئية. أي تعديل مستقبلي للثيم يجب أن يتم في `opal_theme_system.css` فقط.
