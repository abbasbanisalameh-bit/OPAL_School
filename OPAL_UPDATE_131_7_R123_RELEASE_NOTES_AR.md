# OPAL Update 131.7 R123
## Single Theme Authority — Topbar + Dashboard Cascade Lock

### ما تم اكتشافه
الفحص أظهر وجود طبقات CSS تاريخية متعددة للـTopbar ولوحة الرئيسية، وبعضها يضع خلفيات داكنة صريحة. كما أن رابط CSS المشترك كان يستخدم cache key قديمًا، ما يسمح للهاتف بعرض نسخة CSS مخزنة بدل النسخة الجديدة.

### ما تم عمله
- تثبيت طبقة Theme Authority واحدة في نهاية `static/css/opal_theme_system.css`.
- إصلاح منطقة صورة المستخدم + الإشعارات + زر الثيم + البحث كمنظومة واحدة.
- ربط أسطح الصفحة الرئيسية بالثيم المركزي.
- ربط نافذة الوصول السريع بالثيم المركزي.
- منع القواعد الداكنة التاريخية من الفوز في الـcascade.
- تغيير cache key إلى `r123-single-theme-authority`.
- إبقاء الألوان الوظيفية المقصودة لبطاقات KPI والوصول السريع والتنبيهات.
- إبقاء الـinline styles الديناميكية الخاصة بالأبعاد/النسب/موضع الصورة لأنها بيانات تشغيلية وليست نظام ثيم.

### ملاحظة معمارية
Bootstrap يبقى إطار CSS خارجيًا مطلوبًا للتشغيل، لكنه ليس مصدر هوية OPAL. مصدر الهوية المرئية الموحد هو `opal_theme_system.css`.
