# تركيب R124

1. ارفع الملف من مركز التحديثات.
2. نفذ **التحقق** أولًا.
3. تأكد من ظهور:
   - R124
   - `package_revision = 33`
   - `code_only = true`
4. بعد نجاح التحقق نفذ التحديث.
5. أعد تحميل المتصفح تحميلًا كاملًا؛ تم تغيير cache key إلى R124.

## الاختبار بعد التركيب

اختبر بالترتيب:

### 1. الثيم الأخضر
- صورة المستخدم
- البحث
- الإشعارات
- الصفحة الرئيسية
- مؤشرات الرضا: يجب أن يظهر الرقم داخل الدائرة بوضوح.
- البطاقات والجداول.

### 2. الثيم الفاتح
نفس الاختبار.

### 3. الثيم الداكن
نفس الاختبار.

### القاعدة الجديدة
أي تغيير مستقبلي في اللون أو الخلفية أو الحدود أو الظلال أو شكل المكونات العامة يجب أن يكون في:

`static/css/opal_theme_system.css`

ولا نضيف `<style>` جديدًا إلى القوالب، ولا ملف CSS موازيًا، ولا CSS داخل `extra_css`.
